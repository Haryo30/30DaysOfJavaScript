// Loops

/**
 * Loops merupakan struktur kontrol yang memungkinkan kita untuk mengeksekusi blok kode
 * secara berulang-ulang sesuai dengan kondisi yang di tentukan.
 * 
 */

const for_Loop = require('./for_loop')
const while_loop = require('./while_loop')
const for_of_loop = require('./for_of_loop')
const merusak = require('./break')
const melanjutkan = require('./continue')